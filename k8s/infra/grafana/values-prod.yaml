grafana:
  ingress:
    enabled: true
    className: nginx
    hosts:
      - host: grafana.example.com
        paths:
          - path: /
            pathType: Prefix

  prometheus:
    url: "http://infra-prometheus:9090"
