server:
  port: 8087

spring:
  application:
    name: notifications-service
  config:
    import: "configserver:http://config-server:8888"

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh