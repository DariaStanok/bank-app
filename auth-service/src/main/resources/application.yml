server:
  port: 8090

spring:
  application:
    name: auth-service
  config:
    import: "configserver:http://config-server:8888"

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
