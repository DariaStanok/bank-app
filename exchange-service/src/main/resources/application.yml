server:
  port: 8084

spring:
  application:
    name: exchange-service
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
