server:
  port: 8088

spring:
  application:
    name: front-ui-service
  config:
    import: "configserver:http://config-server:8888"

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
