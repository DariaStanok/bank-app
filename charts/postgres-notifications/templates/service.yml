apiVersion: v1
kind: Service
metadata:
  name: postgres-notifications
spec:
  type: ClusterIP
  selector:
    app: postgres-notifications
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgres
