apiVersion: v1
kind: Service
metadata:
  name: postgres-transfer
spec:
  type: ClusterIP
  selector:
    app: postgres-transfer
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgres
