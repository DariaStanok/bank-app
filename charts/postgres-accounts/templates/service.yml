apiVersion: v1
kind: Service
metadata:
  name: postgres-accounts
spec:
  type: ClusterIP
  selector:
    app: postgres-accounts
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgres
