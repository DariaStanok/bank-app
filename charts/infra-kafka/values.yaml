kafka:
  replicaCount: 1

  image:
    registry: docker.io
    repository: bitnamilegacy/kafka
    tag: 3.6

  persistence:
    enabled: true
    size: 1Gi

  zookeeper:
    replicaCount: 1
    persistence:
      enabled: true
      size: 1Gi
