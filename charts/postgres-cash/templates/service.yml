apiVersion: v1
kind: Service
metadata:
  name: postgres-cash
spec:
  type: ClusterIP
  selector:
    app: postgres-cash
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgres
